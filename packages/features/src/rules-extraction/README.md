# @launchstack/features/rules-extraction

Stub — structured rule/policy extraction from ingested documents will live here.

Intent: given regulatory, contractual, or SOP documents, extract actionable
rules (preconditions, obligations, exceptions) into a normalized schema that
downstream features (compliance checking, predictive analysis) can query.
Builds on core ingestion + guardrails; owns the rule schema + extraction
prompts.
