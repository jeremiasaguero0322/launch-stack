# @launchstack/features/workflow-generation

Stub — LLM-generated, reviewable workflow DSL will live here.

Intent: user describes a process in natural language; a structured YAML/JSON
workflow is generated, diffed against prior versions, and stored for
deterministic replay. Shares the core ingestion + guardrails stack; this
package owns the authoring + schema surface.
