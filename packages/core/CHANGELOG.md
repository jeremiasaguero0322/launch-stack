# @launchstack/core

## 0.0.0

- Initial pre-publish scaffold. See the monorepo restructure plan for the
  set of subsystems already in core (DB, LLM, embeddings, OCR, ingestion,
  graph, guardrails, reranking, NER, crypto) and the ports hosts supply
  (StoragePort, JobDispatcherPort, CreditsPort).
