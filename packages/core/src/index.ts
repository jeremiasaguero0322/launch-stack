/**
 * @launchstack/core
 *
 * Scaffolding barrel — individual subsystems are progressively landed as
 * subpath exports (`@launchstack/core/db`, `@launchstack/core/db/schema`,
 * etc.) per the restructure plan.
 */

export {};
